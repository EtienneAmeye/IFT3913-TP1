public class Test {
    //This class is a test

    private int some_variable;  /* a variable */

    private String texte = "Escaping \" ";            // A comment \\
    private String more_texte = "Double slash \\";

    /*

    /*

    // Will count for 3 lines */

    /**
    * This is javadoc
    */
    public Test(int anotherVariable)
    {
        this./*Why does Java accept this?*/some_variable = anotherVariable; /*
        *   Yes, I known this shouldn't work
        */  int n;

        //A if case
        if /*weird comment*/ (condition) {
            break;
        }

        do
        {
            for(statement;statement;statement)  //normal comment
            { ... }

            switch();
        }while();
    }

    /*comment*/ public void function(type argument) {       //comment
        if(condition) { returns; }

        //code
    }
}

//loc: 33       cloc: 18    wmc: 7
